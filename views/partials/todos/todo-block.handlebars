<li class="my-1">
    {{#unless completed}}
        <div class="py-4 px-2 border border-primary complete" data-id="{{id}}" data-completed="{{completed}}">
            {{todo}}
        </div>
    {{/unless}}

    {{#if completed}}
        <div class="py-4 px-2 border border-secondary completed">
            {{todo}}
        </div>
    {{/if}}
</li>
